# Runtime
pyyaml>=6.0
httpx>=0.27

# Quant / statistics core
numpy>=1.26
pandas>=2.2
scipy>=1.11
statsmodels>=0.14      # ADF / CADF cointegration test, OLS, O-U half-life regression
scikit-learn>=1.3      # optional Phase-3 ML classifier layer

# Market calendar (NYSE holidays, early closes, DST) — replaces forex 24/5 assumption
exchange_calendars>=4.5

# Data sources
yfinance>=0.2           # historical bars for research/backtest prototyping
ib_async>=1.0           # IBKR TWS/Gateway API (feed + broker), same as forex-trading

# Model persistence (Phase 3 ML layer)
joblib>=1.3

# Dashboard
fastapi>=0.110
uvicorn>=0.29

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

# Optional Phase-3 ML classifier
# lightgbm>=4.0
